# Commit: 88df944

| Campo | Valor |
|---|---|
| **Fecha** | 2026-05-07 |
| **Autor** | jaime |
| **Rama** | main |
| **Mensaje** | feat: implement AgentSetupWizard, ProviderCard, SetupForm, OpenAILikeProvider |
| **Hash** | 88df944 |

## Decisiones (10 puntos)

1. Wizard valida API Key contra `list_models()` real, no sintaxis local.
2. ProviderCard y SetupForm son componentes puros (sin context, sin I/O).
3. Logo de provider intenta SVG → fallback PNG → si ambos fallan, sin imagen.
4. CSS unificado en `agent-panel.css`, sin archivos CSS extra.
5. Sidebar del wizard fijo 220px, estructura de lista para futura expansión.
6. Botón "Configurar agente" usa animación rainbow con 7 stops cada 3s (sin hue-rotate).
7. Overlay cierra con click fuera o botón X, sin confirmación.
8. `base_url` añadido a `storage-api.ts` para endpoint de OpenAILikeProvider.
9. `setProvider` expuesto en AgentContext para reemplazar DummyProvider post-validación.
10. Cero dependencias npm nuevas — iconos SVG inline, fetch nativo, animaciones CSS puras.

## Archivos (50 files, +5032/-355)

**Creados:** 33 archivos — componentes React, tests, contextos, providers, CSS, specs.
**Modificados:** 12 archivos — `.gitignore`, `electron/ipc.ts`, `electron/preload.ts`, `eslint.config.mjs`, `scripts/git-workflow.sh`, `specs/design_system.md`, `App.tsx`, `App.test.tsx`, `main.tsx`, `App.css`, `renderer.d.ts`, `tsconfig.electron.json`.
**Renombrados:** 2 specs archivados (`agent-panel-original.md`, `agent-panel.md`).
